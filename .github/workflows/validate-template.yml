name: Validate template

on:
  pull_request:
    branches:
      - main
    paths:
      - "plugins/**"
      - ".cursor-plugin/**"
      - "scripts/**"
  push:
    branches:
      - main
    paths:
      - "plugins/**"
      - ".cursor-plugin/**"
      - "scripts/**"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Run template validator
        run: node scripts/validate-template.mjs

      - name: Test worktree patch round-trip
        run: node scripts/test-patch-roundtrip.mjs
