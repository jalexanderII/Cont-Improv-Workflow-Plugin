diff --git a/lib/api/route-handlers.ts b/lib/api/route-handlers.ts
index 6035f968..c8ce97d5 100644
--- a/lib/api/route-handlers.ts
+++ b/lib/api/route-handlers.ts
@@ -394,7 +394,11 @@ export function withCronAuth<TRequest extends Request>(
     if (authError) {
       return authError;
     }
-    return handler(request);
+    try {
+      return await handler(request);
+    } catch (error) {
+      return handleRouteError(error, { routeName: "Cron route error" });
+    }
   };
 }
 
@@ -435,7 +439,11 @@ export function withWebhookAuth<TRequest extends Request>(
         : unauthorizedResponse("Unauthorized");
     }
 
-    return handler(request, { rawBody, verification });
+    try {
+      return await handler(request, { rawBody, verification });
+    } catch (error) {
+      return handleRouteError(error, { routeName: "Webhook route error" });
+    }
   };
 }
 
